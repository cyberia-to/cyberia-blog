---
tags: blog, cyber
date: 2026-03-24
---
polynomial nouns + go-cyber subgraph + kuro bootstrap + cyb mobile + security closed

## research

three research papers dropped: [[cyber/research/polynomial nouns]] (all-algebraic computation and state), [[cyber/research/recursive brakedown]] (the perfect PCS), and [[cyber/research/polynomial proof system]] (foundational definition with 22 wiki-links). updated [[cyber/research/algorithmic essence of superintelligence]] with polynomial nouns + recursive Brakedown. decider jet: 89-constraint verification of all history.

resolved the small noun question: PCS for everything, no threshold.

## stack

[[kuro]] bootstrapped from zero — F₂ tower field arithmetic from F₂ to F₂¹²⁸. subgraph declared in cyber graph.

stack pages updated: [[zheng]] (Whirlaway → SuperSpartan + WHIR), [[bbg]] (polynomial nouns integrated, recursive Brakedown numbers cascaded, docs rewritten from first principles), [[nebu]] (extension fields), [[hemera]] (polynomial nouns integrated, removed batched-proving and folded-sponge — polynomial nouns make them unnecessary).

[[nox]] specs: state operations explicitly derived from 16 patterns. state jets as third category. polynomial nouns integrated across reference.

[[zheng]] reference rewritten from first principles — no versions, no evolution. PCS decomposed into trait + two named backends. 102 stale references replaced in explanation docs. recursive Brakedown integrated.

[[bbg]] 8 executed roadmap proposals removed (content moved to reference + explanation). critical reference issues fixed.

[[go-cyber]] declared as subgraph (33 docs pages).

[[trident]] oriented toward cyber stack. polynomial target roadmap for nox engine.

## cyb

repo restructured: bevy at root, react/leptos/nu as subprojects. empty cyb-ui (Dioxus placeholder) removed.

unified CI: build all platforms with smart change detection. 6 CI fixes (WebKitGTK deps, SSH deploy key, Rust flags, dead_code, gradle-wrapper).

release process documented: kelvin+semver, testing tiers, OTA architecture.

mobile web live. own wallet: AES-256-GCM, PBKDF2 1M iterations, Ledger support, 15-min auto-lock. 31 security audits, 108 findings, 0 open. audit 32: mnemonic masking, service worker cache filtering, clipboard auto-clear.

## subgraph migrations

[[foculus]], [[tru]], [[cybergraph]] — migrated graph/ articles to reference/ specs and docs/ explainers.

[[optica]] — fix: warn on YAML parse errors instead of silently ignoring frontmatter.

~60 commits across 15 repos.
